<UserControl
    x:Class="Huaban.UWP.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Huaban.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:HB="using:Huaban.UWP.Controls"
	HorizontalAlignment="Stretch"
	VerticalAlignment="Stretch"
    mc:Ignorable="d">
	<StackPanel x:Name="rootPanel" RequestedTheme="{Binding Theme}" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<HB:TitleControl VerticalAlignment="Top"/>
		<StackPanel Width="240" HorizontalAlignment="Center" Margin="12">
			<StackPanel Margin="0 12">
				<TextBox BorderThickness="0 0 0 1" PlaceholderText="用户名/邮箱地址" Text="{Binding UserName, Mode=TwoWay}"></TextBox>
				<PasswordBox BorderThickness="0 0 0 1" PlaceholderText="密码" Password="{Binding Password, Mode=TwoWay}"></PasswordBox>
			</StackPanel>
			<Grid VerticalAlignment="Bottom" RequestedTheme="Dark">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"></ColumnDefinition>
					<ColumnDefinition Width="3"></ColumnDefinition>
					<ColumnDefinition Width="*"></ColumnDefinition>
				</Grid.ColumnDefinitions>
				<Button HorizontalAlignment="Stretch" BorderThickness="0" Background="{StaticResource HuabanColorBrush}" Content="登录" Command="{Binding LoginCommand}"></Button>
				<Button HorizontalAlignment="Stretch" BorderThickness="0" Background="{StaticResource HuabanColorBrush}" Content="取消" Command="{Binding CancelCommand}" Grid.Column="2"></Button>
			</Grid>

		</StackPanel>
		<ProgressRing IsActive="{Binding IsLoading}"></ProgressRing>
	</StackPanel>
</UserControl>
