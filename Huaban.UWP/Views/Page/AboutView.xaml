<HB:HBPage
    x:Class="Huaban.UWP.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:HB="using:Huaban.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{d:DesignInstance }"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <HB:TitleControl VerticalAlignment="Top"></HB:TitleControl>
        <ListView Grid.Row="1" ItemsSource="{Binding AboutList}" Style="{StaticResource AboutListStyle}"></ListView>
    </Grid>
</HB:HBPage>
